#include <stdio.h>

int main() {
    int n, i;

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Sequential Allocation:\n");

    for(i = 0; i < n; i++) {
        printf("Record %d -> Block %d\n", i + 1, i);
    }

    return 0;
}

